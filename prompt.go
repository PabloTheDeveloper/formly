package main

type prompt struct {
	sequence    int64
	flag, usage string
}

// TODO add stubs for functions

// validate flag (make sure word, and length)
// validate usage (make sure length (same as ksat)

// convert, parse, from string into prompt

// dbInsert (for prompt) (duplicates allowed)
